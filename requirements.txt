fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
alembic>=1.13.0
pymysql>=1.1.0
pydantic-settings>=2.2.0
authlib>=1.3.0
cryptography>=41.0.0  # required by authlib for RS256/EC JWT decoding (Authentik uses RS256)
passlib[bcrypt]>=1.7.4
bcrypt<4.0  # passlib 1.7.4 is incompatible with bcrypt>=4.0
jinja2>=3.1.3
itsdangerous>=2.1.0
starlette>=0.36.0

typst>=0.11.0
mt-940>=5.1.1  # MT940 bank statement parsing (ledger import); CAMT.053 is parsed with stdlib xml only
fints>=5.0.0  # manual FinTS bank statement live-pull (ledger import, Phase 3); no stdlib alternative for the FinTS/HBCI protocol
