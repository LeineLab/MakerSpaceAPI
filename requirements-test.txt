# Test-only dependencies — never installed in the production image (Dockerfile
# only installs requirements.txt). CI installs this on top of requirements.txt
# after a smoke-testing the app with production dependencies alone.
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-playwright>=0.4.0
